# Security policy

Do not create public issues containing bearer tokens, pairing secrets, full device addresses, private IP addresses or screenshots that expose them.

For a security issue, contact the repository maintainer privately with:

- affected InputBridge version
- impact
- reproduction steps
- any suggested mitigation

InputBridge is designed for trusted private networks. Do not expose its controller port directly to the internet.
